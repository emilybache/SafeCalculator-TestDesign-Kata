const test = require('tape')
const Calculator = require('../calculator')

test('should not throw when authorized', (t) => {
  // TODO: write a test that fails due to the bug in
  // Calculator.divide()
  t.fail("TODO: implement test")
  t.end()
})
